import { styled } from "nativewind";
import { View } from "react-native";

export const StyledPanel = styled(
  View,
  "flex space-y-4 divide-y divide-gray-200 overflow-hidden rounded-lg bg-white shadow",
);
